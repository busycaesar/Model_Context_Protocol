[project]
name = "server-firebase"
version = "0.1.0"
description = "This is an MCP server to work with firebase."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.6.0",
]
